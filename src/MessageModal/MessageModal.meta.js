import MessageModal from './MessageModal';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(MessageModal);

metadata.exportedFrom({
  path: 'src/MessageModal/MessageModal.js',
});
