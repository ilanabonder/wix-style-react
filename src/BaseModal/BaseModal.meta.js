import BaseModal from './BaseModal';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(BaseModal);

metadata.exportedFrom({
  path: 'src/BaseModal/BaseModal.js',
});
